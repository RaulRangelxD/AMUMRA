import { Alianzas } from '@/components/pages/nosotras/Alianzas'
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Alianzas',
}

export default function AlianzasPage() {
  return <Alianzas />
}
